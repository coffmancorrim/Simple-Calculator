package com.example.calculator;

import javafx.scene.control.TextField;

public interface Output {
    final static TextField output = new TextField("");
    final static Calculate calculator = new Calculate();
}
